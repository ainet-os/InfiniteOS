# 前端开发端口与后端代理地址（可选，不配置则使用生产默认值）
# 生产环境默认：前端 8080，后端 3000
# 测试环境示例：前端 8090，后端 3001（复制为 .env 后修改）
# VITE_DEV_PORT=8090
# VITE_DEV_BACKEND_URL=http://localhost:3001
# VITE_API_BASE_URL=http://localhost:8090/api
